<h1>Show all the user that are going to attend to the events</h1>

<% @event.attendences.each do |a| %>
   <%= User.find(a.event_attende_id).username %><br>
   <%= link_to 'Attendee Details', user_path(User.find(a.event_attende_id).id) %>
<%end%>